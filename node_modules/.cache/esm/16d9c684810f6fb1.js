let Sector;_810‍.x([["sectors",()=>sectors],["addSector",()=>addSector],["deleteSector",()=>deleteSector]]);_810‍.w("../../models/Sector",[["default",["Sector"],function(v){Sector=v}]]);

// * Query
// List sectors depend on type(can be null)
       const sectors = async (_, { type }) => {
	try {
		let sectors;
		if (type) sectors = await Sector.find({ type });
		else sectors = await Sector.find({});
		return sectors;
	} catch (e) {
		_810‍.g.console.log(e);
	}
};

// * Mutation
// Add sector with name, type
       const addSector = async (_, { sectorInput }) => {
	try {
		const { basic, detail } = sectorInput;
		let sector;

		if (basic) {
			sector = new Sector({
				type: 'basic',
				name: basic,
			});

			await sector.save();
		}
		if (detail) {
			sector = new Sector({
				type: 'detail',
				name: detail,
			});

			await sector.save();
		}

		return sector;
	} catch (e) {
		_810‍.g.console.log(e);
	}
};

// Delete sector with name, type
       const deleteSector = async (_, { sectorInput }) => {
	try {
		const deletedSector = await Sector.findOneAndDelete({ ...sectorInput });
		return deletedSector;
	} catch (e) {
		_810‍.g.console.log(e);
	}
};
