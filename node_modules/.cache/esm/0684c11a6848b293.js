let gql;_684‍.x([["default",()=>_684‍.o]]);_684‍.w("apollo-server",[["gql",["gql"],function(v){gql=v}]]);

const user = gql`
	type User {
		username: String!
		password: String!
	}

	input userInput {
		username: String!
		password: String!
		passwordCheck: String
	}

	type Check {
		id: ID!
		username: String!
	}
`;

_684‍.d(user);
