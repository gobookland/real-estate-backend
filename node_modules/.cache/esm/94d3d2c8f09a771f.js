let mongoose,bcrypt,jwt;_f09‍.x([["default",()=>_f09‍.o]]);_f09‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_f09‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);_f09‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);



const { Schema, model } = mongoose;

const UserSchema = new Schema({
	username: String,
	password: String,
});

UserSchema.statics.findByUsername = function(username) {
	return this.findOne({ username });
};

UserSchema.methods.generateToken = function() {
	const token = jwt.sign(
		{
			_id: this._id,
			username: this.username,
		},
		process.env.JWT_SECRET,
		{
			expiresIn: '7d',
		},
	);

	return token;
};

UserSchema.methods.passwordCheck = async function(password) {
	return await bcrypt.compare(password, this.password);
};

UserSchema.pre('save', async function(next) {
	const hashedPassword = await bcrypt.hash(this.password, 10);
	this.password = hashedPassword;
	next();
});

_f09‍.d(model('user', UserSchema));
